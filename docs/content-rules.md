# Правила контента, стиль речи и frontmatter

Полные правила редполитики. Краткая сводка — в `CLAUDE.md`. При
сомнениях по конкретному случаю — `docs/editorial-policy.md`
(классы A/B/C для фактчека).

## Структура статьи

- **Длина опорных статей** 1500–2500 слов. Сателлитов — 800–1500.
- **Лид** — конкретный факт или вопрос, не «X является важным
  инструментом». Указываем, что изменилось / когда / кого касается.
- **4–6 H2-секций.** Тема, не вопрос. «Сроки подачи уведомлений», а
  не «Каковы сроки подачи уведомлений?». Форма «Каков/Каковы/Что
  такое» запрещена.
- **FAQ блок** — 3–5 вопросов внизу. Заголовок `## Частые вопросы`.
  Каждый вопрос — реальная формулировка из выдачи.
- **Заключение / Итог** — только если добавляет новое (шаги «что
  делать прямо сейчас»). Раздел, повторяющий лид, удаляем.
- **Горизонтальные разделители `---`** внутри тела статьи — не
  использовать, дробят текст в сборник FAQ.
- **Перелинковка на Контур:** минимум 3 ссылки на статьи Контура (kontur.ru)
  из `src/data/kontur-links.json` по совпадению темы. Где подходящей статьи
  нет — текстовая заглушка `тема (нужна ссылка на статью Контура про «тема»)`.
  На наш `/blog/` и `/category/` не ссылаемся (проект работает во благо Контура).

## Источники

Факты, цифры и нормы — со ссылкой на первоисточник в драфте (НПА,
разъяснения ведомств, «Честный знак»). Голословных утверждений быть
не должно.

**Правовая оговорка:** публикуем не как юридическую консультацию, а
как информационный материал — об этом сказано в `BlogPost.astro`.

## Категории и теги

- **Categories:** одна основная из `CATEGORIES` в `src/consts.ts`:
  `ts-piot`, `markirovka`, `zakonodatelstvo`, `kkt`, `egais`
- **Tags:** 4–7 штук, лоркейс, по делу. Новый тег автоматически
  создаёт страницу `/tag/<slug>/`.

## Frontmatter-шаблон

```yaml
---
title: "Заголовок: что и почему"
description: "Краткое описание для SERP, до 160 символов."
pubDate: "YYYY-MM-DD"
updatedDate: "YYYY-MM-DD"   # опционально
reviewDate: "YYYY-MM-DD"    # опционально — плановая дата следующей проверки фактов
tags:
  - тег1
  - тег2
categories:
  - ts-piot   # или markirovka, zakonodatelstvo, kkt, egais
draft: false
seo:
  keywords:
    - целевой ключ 1
    - целевой ключ 2
cpa: 'kontur-ofd'           # опционально, id из src/data/cpa-banners.ts
heroImage: "/images/hero/SLUG.jpg"  # опционально, иначе Satori
---
```

## SEO-требования

- **Целевой ключ** — в title, первом абзаце и хотя бы одном H2.
- **Description** 140–160 символов, с ключом и ценностным крючком.
- Семантическая разметка `Article` + `BreadcrumbList` подставляется
  автоматически через `BlogPost.astro`. `articleSection` и `keywords`
  берутся из frontmatter.
- **Канонический URL** — автоматически. `seo.canonical` использовать
  только если материал — копия другого источника.
- **heroImage** в формате 2:1, ≥ 1200×600 — необязателен. Без него
  для `og:image` подставляется PNG Satori (`/og/<slug>.png`).
  Подробности — `docs/images.md`.

## Стиль речи

### Основные принципы

- **Активный залог,** прямые формулировки, минимум канцелярита.
- Никаких эмодзи, кликбейта, восклицательных знаков.
- **Числа — цифрами;** сроки — конкретно (01.07.2026, не «в середине 2026»).
- **Прямое обращение к читателю:** «проверьте», «настройте», «подайте» —
  не «предпринимателям необходимо» или «следует».
- Аудитория — занятый предприниматель: каждое предложение даёт
  информацию, не разгоняет контекст.

### Термины

- **Юридические термины** используем точно: «контрольно-кассовая
  техника» (а не «кассовый аппарат»), «универсальный передаточный
  документ», «налогоплательщик УСН».
- **Разговорные термины,** которые аудитория реально использует, —
  допустимы: «вейпы» (не «электронные системы доставки никотина»),
  «касса» в бытовом контексте. Но не «молочка» — пишем «молочная
  продукция».
- **Сокращения** раскрываем при первом упоминании: ТС ПИоТ, УКЭП,
  ГИС МТ, 54-ФЗ.
- **«Честный знак»** — всегда в кавычках; «система маркировки
  «Честный знак»» при первом упоминании, далее просто ««Честный знак»».

### Запрещённые слова и обороты

Запускать перед публикацией:

```
grep -rni "является\|каковы\|каков \|следует отметить\|необходимо отметить\|таким образом,\|в данном контексте\|выглядит следующим образом\|с точки зрения\|предпринимателям стоит\|предпринимателям необходимо\|в целях\|осуществляет\|осуществляется\|насыщен" src/content/blog/
```

| Паразит | Замена |
|---|---|
| `является` | активный глагол или тире: «X — это Y» |
| `осуществляет/осуществляется` | делает / передаёт / проводит |
| `следует/необходимо отметить` | удалить, сказать прямо |
| `таким образом,` | удалить вводное слово |
| `в данном контексте` | удалить |
| `выглядит следующим образом` | удалить, поставить двоеточие |
| `с точки зрения` | переформулировать конкретно |
| `предпринимателям стоит/необходимо` | прямое обращение: «проверьте» |
| `в целях` | `чтобы` |
| `насыщенный` (о периоде) | назвать конкретно, что изменилось |
| `молочка` | `молочная продукция` |

### Что перенимаем у сильных конкурентов (бенчмарк Контур)

Сравнение с Контур.Маркетом по нашим темам (полный разбор —
`docs/competitor-benchmark-kontur.md`) дало набор приёмов. Берём лучшее,
сохраняя наши границы: без рекламных CTA в теле, без штампов, без опечаток.

- **Тон чуть теплее.** Кроме сухой деловой подачи допускаем живые прямые
  мостики к читателю в переходах: «Смотрите, нужна ли касса для вашего случая»,
  «Проверьте по таблице». Живость — через обращение и примеры, не через воду.
- **Оглавление «В этой статье»** после лида — список H2-секций. Навигация +
  сниппеты в выдаче.
- **E-E-A-T:** `author` — из `src/data/authors.json` (реальный, не дефолт),
  `updatedDate` — дата актуализации. Именной эксперт — только реальный человек,
  выдумывать нельзя.
- **FAQPage-разметка:** вопросы-ответы FAQ дублируем в поле `faq[]` frontmatter
  (краткие ответы 1–3 предложения) — для сниппетов в выдаче.
- **Калибровка голоса** (так/не так) — таблица в `content-writer.md`: тёплый и
  точный, без казёнщины и пассива.
- **Полные штрафные блоки:** все субъекты (ИП / должностное лицо / юрлицо),
  смягчение для МСП (ст. 4.1.2 КоАП — вдвое меньше), уголовная статья где есть.
- **Наши границы, которые НЕ сдаём:** никакой прямой рекламы решений в теле,
  никаких «молочка»/опечаток/«Источник: ChatGPT», актуальность на текущий год
  (ТС ПИоТ, ФФД 1.2, свежие редакции КоАП) — по этому мы конкурента обходим.

---

## CPA-врезка в теле статьи

Каждая статья должна содержать **ровно одну** inline CPA-врезку
`<CpaCallout id="..." />` в середине тела — после раскрытия проблемы,
перед или внутри блока с решением. Это главный конвертер: CTR в 5–10 раз
выше, чем у баннера в подвале статьи.

**Формат файла:** `.mdx`, не `.md` (для поддержки JSX-компонента).
В шапке после frontmatter:

```mdx
import CpaCallout from '../../components/CpaCallout.astro';
```

**Куда вставлять.** После 2-й или 3-й H2-секции, когда читатель уже
понял суть боли и переходит к решению. Не в самое начало (ещё не дочитал
до боли) и не в конец (там layout сам показывает финальный баннер).

**Какой id брать.** По полю `categories` во frontmatter — таблица
соответствий в `.claude/agents/content-writer.md` раздел «CPA-врезка».

**Подводка.** Один абзац перед `<CpaCallout>` в стиле «Если разбираться
в одиночку времени нет — есть готовое решение». Без рекламных эпитетов.

**Дисклеймер 38-ФЗ.** Компонент сам рисует «Реклама · erid: …» под
блоком, если у баннера есть erid в `src/data/ord-erids.json`. Вручную
ничего не пишем.

**Контроль:** в подвале статьи показывается ещё один CPA автоматически
(из `CATEGORY_DEFAULT_CPA` или поля `cpa` frontmatter). Это нормально —
два CPA на статью (один inline, один в подвале) — целевая раскладка.
Третий не нужен.

---

## Социальные черновики

Хранятся в `src/content/wiki/social/` (канонический путь) или
`src/content/social/` (старый путь для постов 2026-05-01). Один
файл на статью.

```
src/content/wiki/social/YYYY-MM-DD-slug.md
```

### Frontmatter соцчерновика

```yaml
---
title: "Название статьи (копия)"
slug: "YYYY-MM-DD-slug"        # совпадает с именем файла блога
articleUrl: "/blog/slug/"
status: draft                   # draft | ready | posted
createdDate: "YYYY-MM-DD"
---
```

### Структура файла

```markdown
## Telegram

[600–900 знаков, один тезис, без заголовков]

etiketka-media.ru/blog/<slug>/

---

## VK

[900–1400 знаков, 2–3 абзаца, 3–5 хэштегов в конце]

---

## Дзен

**Заголовок Дзен:** ...

[Лид-блок 2–3 абзаца]

### H2 №1 — угол подачи, отличный от сайтовой статьи
[3–5 абзацев по 3–5 предложений]

### H2 №2
[3–5 абзацев, мини-кейс или практика]

### H2 №3
[3–5 абзацев, разбор частного случая]

### H2 №4 (опционально)
[3–5 абзацев, чек-лист или сравнение]

### Что запомнить
[4–6 буллетов]

---
Полная нормативная база — в материале на Этикетка: [URL]

#хэштег1 #хэштег2 #хэштег3

[Объём: целевой 4500–5500, абсолютный коридор 3500–6000. Меньше 3500 — алгоритм Дзена не покажет. Больше 6000 — теряется дочитываемость.]

---

## Email

**Тема:** ...
**Прехедер:** ...

[300–500 знаков основного текста + CTA-кнопка]
```

### Правила тона по платформам

- **Telegram**: сухо и по делу — аудитория читает в ленте, некогда.
  Первые 2 строки должны цеплять без «дорогой читатель». Никаких
  эмодзи. Последняя строка — всегда ссылка на статью:
  `etiketka-media.ru/blog/<slug>/`.
- **VK**: чуть теплее, можно риторический вопрос в начале. Хэштеги
  — только релевантные (`#маркировка`, `#УСН`, `#онлайнкасса`).
- **Дзен**: лонгрид-платформа, не пост в ленте. Целевой объём
  4500–5500 знаков, абсолютный коридор 3500–6000. Меньше 3500 —
  не выйдет в ленту, больше 6000 — провал по дочитываниям. 4–5 H2-заголовков, абзацы по 3–5 предложений,
  обязательно хотя бы один кейс/диалог/конкретная сумма штрафа.
  Алгоритм Дзена ранжирует по дочитываниям — короткий текст просто
  не выйдет в ленту. Заголовок отличается от сайтового H1
  (вопрос, кейс или срок).
- **Email**: тема письма — выгода или вопрос ≤ 50 символов.
  Прехедер раскрывает. Один CTA, не два.

---

## Чего НЕ публикуем

- Прямую коммерческую рекламу решений (не делаем «купи это ПО»).
- Контент без проверки на действующие нормативные акты.
- Статьи с целевым запросом, под который уже есть материал в
  кластере, — обновляем существующий, а не плодим дубли.
