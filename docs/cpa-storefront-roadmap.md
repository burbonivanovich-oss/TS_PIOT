# Roadmap: витрина продуктов Контура (CPA-storefront)

План превращения портала из «информационка + одиночные CPA-баннеры»
в полноценную витрину набора продуктов Контура. Фаза 1 — только
заявки (лиды), без онлайн-оплаты. Онлайн-оплата вынесена в задел
(раздел «Отложено»).

Статус: фаза 1 (заявки) — воронка витрины собрана (подбор → каталог →
сравнение → бандл → лид-форма), вся аналитика на заявки заведена.
Осталось только то, что блокировано внешними вводными.

**Что уже в коде:**
- `src/data/cpa-banners.ts` — `CATALOG_GROUPS` (4 раздела), `PRODUCT_CATALOG`
  (11 живых продуктов с erid) + 6 draft-офферов без erid (не показываются).
- `src/components/KonturLeadForm.astro` — лид-блок с триггерами доверия;
  CTA-механизм через `CpaCallout`, слот `widget` под официальный виджет
  заявки Контура. Трекинг показа и клика (`lead-form-view`,
  `lead-form-click`, параметр `product`).
- `src/pages/produkty/index.astro` — витрина в формате интернет-магазина
  (по образцу workhardmarish.github.io/kontur-feed): тёмный hero с табами
  категорий и баром преимуществ; секции по типам товара; карточки с
  фото/ценой и кнопкой «В корзину»; карточки комплектов с бейджем «Скидка»,
  зачёркнутой ценой и зелёной «Купить комплект»; CTA-баннер; корзина на
  localStorage (добавление, количество, сумма, «Оформить заявку» → экран
  успеха, без оплаты). Кнопки входа в `/podbor/` и `/sravneniya/`.
- `src/pages/produkty/tovar/[slug].astro` — подробная страница товара:
  фото, цена (наличие, дата обновления, для комплектов — зачёркнутая +
  «Скидка»); для касс — конфигуратор «Скомплектуйте кассу» (отрасль,
  тариф, фискальный накопитель и ОФД из реальных позиций фида, живой
  итог); кнопки «В корзину» (добавляет кассу + выбранные ФН/ОФД, ведёт
  в `/produkty/#cart`) и «Получить консультацию»; преимущества; табы
  Описание / Характеристики / Доставка; блок «С этим покупают»
  (кросс-селл). Генерируется по `slug` товаров из фида.
- `src/data/shop-specs.ts` — подробные описания и таблица характеристик
  товаров (тип устройства из названия, 54-ФЗ/ОФД/маркировка/ЕГАИС через
  Контур, гарантия и условия; ТТХ «железа» не выдумываются).
- Индекс магазина: поиск по каталогу, фильтр по цене, бренду и
  сортировка (по разделам / дешевле / дороже / по названию), JSON-LD `ItemList`.
- `src/components/CartDrawer.astro` — глобальная корзина (подключена в
  Footer): значок в шапке открывает оверлей на любой странице,
  добавление с карточек и страниц товаров через делегирование/события.
- `src/components/QuickOrder.astro` — «Купить в 1 клик»: глобальная
  мини-заявка на один товар (телефон → успех, цель `quick-order`).
  Кнопки на карточках каталога и страницах товаров.
- `src/components/RecentViewed.astro` — блок «Недавно смотрели»
  (localStorage): на витрине и страницах товаров.
- Страницы товаров содержат JSON-LD `Product`/`Offer` и `BreadcrumbList` (SEO).
- Конфигуратор кассы сохраняет выбор (ФН, ОФД, отрасль, тариф) в URL —
  ссылкой на собранный «комплект под ключ» можно поделиться (кнопка
  копирования). `sitemap.xml` включает витрину, товары, сервисы и сравнения.
- `.github/workflows/shop-feed-sync.yml` — еженедельно (и по dispatch)
  перегенерирует `shop-catalog.ts` из фида и коммитит обновлённые цены.
- `src/components/Header.astro` — значок корзины со счётчиком на всех
  страницах (читает localStorage, ведёт в `/produkty/#cart`).
- `src/data/shop-catalog.ts` — данные магазина: товары/цены/фото из фида
  (кассы, ФН, ОФД, ЭДО, периферия, комплекты) + наши сервисы Контура
  секцией из `STOREFRONT_PRODUCTS` (gated по erid, цена-заглушка).
  Генерируется `scripts/shop/sync-feed.mjs` (фид → TS, цены с датой).
- `src/pages/produkty/[slug].astro` — страница продукта (что внутри, кому,
  лид-блок, перелинковка в категории и соседние продукты).
- `src/pages/podbor/` + `src/components/interactive/ServicePicker.tsx` —
  квиз-подбор сервиса (LAB1 #2): 3 шага, дорожка прогресса, результат →
  `/produkty/<slug>/`. Трекинг `podbor-started/result/cta-click`.
- `src/pages/sravneniya/` + `src/data/comparisons.ts` — sticky-таблицы
  сравнения (LAB1 #12, F-27): `kassy-i-ofd`, `edo-i-kadry`. CTA →
  страница продукта, трекинг `sravnenie-cta-click`.
- `src/components/ProductBundle.astro` — бандл «набор под задачу»
  (раздел 10.3), встроен в `/scenario/kofeynya-za-30-dney` и
  `/scenario/perekhod-ip-na-ooo-za-60-dney`. Трекинг `bundle-cta-click`.
- `src/data/metrika/goals.json` — 7 целей воронки витрины (раздел 6).
- `NAV_LINKS` — пункт «Сервисы» → `/produkty/`.

**Что осталось в фазе 1 (блокировано внешними вводными):**
- Получить HTML официального виджета заявки Контура и вставить в слот
  `widget` `KonturLeadForm` (тогда заявка оформляется inline). До этого —
  fallback на `CpaCallout` (уход по ссылке с тегом).
- Зарегистрировать в ОРД 6 draft-офферов (erid) и добавить в `PRODUCT_CATALOG`
  (ЭП/УЦ, Меркурий, Зарплата, НДС+, Призма, Документы).
- Синхронизировать новые цели со счётчиком: `scripts/metrika/sync-goals.mjs`.

---

## 1. Модель: агрегатор поверх биллинга Контура

Сайт статический (Astro → GitHub Pages), своего бэкенда нет. Поэтому
портал не принимает платежи, не хранит лиды и не выступает продавцом.
Роль — лучшая в нише воронка под продукты Контура. Всё, что «продаёт»,
живёт на стороне Контура.

Два механизма монетизации (термины не путать):

| Механизм | Что происходит | Где деньги |
|---|---|---|
| **Лид-форма с тегом** | Клиент оставляет заявку, не уходя с сайта. Заявка падает в кабинет партнёра с кодом `f74746`. Контур закрывает сделку. | CPA за продажу по лиду |
| **Онлайн-оплата (отложено)** | Партнёр генерирует ссылку на оплату/счёт Контура. Оплата идёт на `kontur.ru`. | CPA за оплату |

Фаза 1 использует **только лид-формы**. Это снимает проблему недоверия:
портал не имитирует свой магазин, заявку обрабатывает федеральный бренд.

Подтверждённая механика партнёрской программы Контура:
- встраиваемые **виджеты формы заявки** (HTML/iframe с партнёрским кодом,
  заявки уходят с тегом) — ложатся на статику без бэкенда;
- вознаграждение **5–50 %** в зависимости от продукта;
- ссылки на онлайн-оплату/счёт (для будущей фазы).

Источники: [kontur.ru/partnership](https://kontur.ru/partnership),
[kontur.ru/partnership/online](https://kontur.ru/partnership/online),
[support.kontur.ru/referral](https://support.kontur.ru/referral/38994-registraciya_v_programme).

## 2. Что уже есть в репозитории

- `src/data/cpa-banners.ts` — фактически готовая **база продуктов**:
  13 живых офферов Контура (ОФД, Маркет, Эльба, Экстерн, Фокус, Диадок,
  КЭДО, МЧД, Банк+Эльба и др.) с тегом `f74746` и автоматической
  маркировкой по 38-ФЗ через `erid` из `ord-erids.json`.
- `src/components/CpaCallout.astro` — рендер одного оффера по `id`,
  маркировка рекламы (`rel="sponsored"`, плашка `erid`) автоматическая.
- `CATEGORY_DEFAULT_CPA` / `CATEGORY_DEFAULT_CPA2` — дефолтный оффер
  по категории статьи.
- Сценарии (`/scenario/*`) и интерактивы (`UsnNdsCalc`,
  `PenaltyCalculator`, квизы) — готовые точки встройки.

То есть данные под каталог уже описаны. Не хватает страниц-витрин,
компонента-обёртки для виджета заявки и блоков-бандлов.

## 3. Связь с существующими планами

Чтобы не плодить дубли — этот roadmap опирается на уже отобранные блоки
из `docs/blocks-roadmap.md`:

- **LAB1 #2 — Quiz «подбор сервиса»** (`/podbor/`) — верхняя часть
  воронки: подбирает продукт под бизнес, ведёт на страницу продукта.
- **LAB1 #12 — Comparison table (F-27)** (`/sravneniya/`) — sticky-таблица
  сравнения, конверсионный инструмент. Страница продукта линкуется в неё.
- **Inline vs popup** (`docs/blocks-roadmap.md`, раздел про лид-формы) —
  обоснование, что inline-форма конвертит кратно выше ухода по ссылке.

Витрина — это «середина» воронки между подбором/сравнением и заявкой.

## 4. Фаза 1 — только заявки

### 4.1 Компонент `KonturLeadForm.astro` (ядро фазы)
Обёртка над официальным виджетом заявки Контура. Принимает `productId`
(ключ из `CPA_BANNERS`), подставляет нужный виджет/iframe с тегом,
рисует триггеры доверия (аккредитация, «1,5 млн компаний», логотип),
маркировку по 38-ФЗ (`erid`). Встраивается в статьи (F26-блок),
спецстраницы и страницы продукта.

Критично: показ виджета рекламного оффера **без `erid`** — нарушение
38-ФЗ. Гейт на сборке: если у оффера нет `erid`, форма не рендерится
(как уже сделано для банковских заготовок в `cpa-banners.ts`).

### 4.2 Раздел-каталог `/produkty/`
- Индекс: карточки продуктов, сгруппированные по задачам бизнеса —
  касса и фискализация (Маркет, ОФД, ТС ПИоТ), бухгалтерия и отчётность
  (Эльба, Экстерн), ЭДО и кадры (Диадок, КЭДО, МЧД), проверки и риски
  (Фокус), деньги (Банк+Эльба).
- Страница продукта `/produkty/<id>/`: что это, кому, тариф/условия,
  `KonturLeadForm`, перелинковка в статьи кластера и в `/sravneniya/`.
- Источник данных — `CPA_BANNERS`. Расширить интерфейс `CpaBanner`
  опциональными полями для витрины (целевая аудитория, буллеты выгод,
  группа каталога), не ломая текущий рендер баннеров.

### 4.3 Бандлы под сценарий
Блок «набор продуктов под задачу» в существующих сценариях
(`/scenario/kofeynya-za-30-dney`, `perekhod-ip-na-ooo`): 3–4 связанных
оффера одним блоком (кофейня → касса+ТС ПИоТ + ОФД + Эльба + Диадок).
Поднимает средний чек, попадает в реальную потребность. Зачаток —
связка-оффер `bank-elba` в данных.

### 4.4 Перелинковка воронки
`/podbor/` (quiz) → страница продукта `/produkty/<id>/` → `/sravneniya/`
(если есть сравнение) → лид-форма. Из статей кластера — ссылки на
профильные страницы продуктов и бандлы.

## 5. Лечение недоверия (даже в фазе заявок)

- Не имитировать свой магазин. Формулировки честные: «оставить заявку —
  Контур перезвонит», «оформление на kontur.ru, договор с Контуром».
- Триггеры доверия рядом с формой: аккредитация оператора, число
  клиентов, логотип, видимая маркировка рекламы.
- Дорогие/сложные продукты (Маркировка под ключ, ЭДО-интеграция) —
  только заявка; они и так продаются через звонок менеджера.

## 6. Метрики и проверка гипотез

- Цели Метрики на отправку заявки по каждому виджету (см. `docs/metrika.md`,
  `src/data/metrika/goals.json`). Обёртка `src/utils/track.ts` —
  `reachGoal` на submit.
- Сравнить конверсию inline-формы против старого «ухода по ссылке»
  (`CpaCallout` с внешним `ctaHref`) на одном и том же оффере.
- Решение о фазе 2 (онлайн-оплата) — по данным фазы 1: если конверсия
  в заявку и доходимость до сделки устраивают, добавлять оплату точечно.

## 7. Отложено (не делаем сейчас)

- **Онлайн-оплата прямо на сайте** — кнопка с готовой ссылкой оплаты
  Контура. Подключать после замера фазы 1, начиная с дешёвых понятных
  продуктов (ОФД, Фокус). Платёж — на домене Контура, не у портала.
- **Прайс-калькулятор «собери тариф»** с выводом ссылки на оплату.
- **Реселлерская модель** (продажа от своего ИП, свои счета) —
  отклонено: другой бизнес (биллинг, поддержка, возвраты), убивает
  скорость и доверие. Реферальная модель даёт те же 5–50 % без этого.
- **Банковские партнёрки** (Т-Банк, Точка, Альфа) — заготовки в
  `cpa-banners.ts` без `erid` и без партнёрских ссылок. Не показывать,
  пока не одобрены заявки и не зарегистрированы креативы в ОРД.

## 8. Открытые вопросы

- Точный формат виджета Контура: iframe или вставка скрипта. Нужен
  HTML из кабинета партнёра по каждому продукту.
- Какие из 13 офферов имеют готовый виджет заявки, а какие — только
  реферальную ссылку (для них фаза 1 = `CpaCallout`, без inline-формы).
- erid: проверить покрытие в `ord-erids.json` для офферов, идущих в
  витрину (`scripts/ord-register.mjs`).

---

## 9. Полная линейка Контура → маппинг на наши кластеры

Экосистема Контура — около 50 сервисов. Портал про кассу, маркировку и
законы для МСБ берёт не всё, а релевантное ядро. Тиры релевантности:

- **S** — прямо наша тема, идёт в витрину в первую очередь.
- **A** — смежное, регулярно всплывает в законах для МСБ.
- **B** — ситуативно, под отдельные статьи/сценарии.
- **C** — вне фокуса портала, в витрину не берём.

Категории-кластеры (источник истины — `src/consts.ts`):
`ts-piot`, `markirovka`, `kkt`, `egais`, `zakonodatelstvo`.

### Группа A. Касса, фискализация, маркировка (ядро портала)

| Продукт | Что | Тир | Кластеры | Оффер в `cpa-banners.ts` |
|---|---|---|---|---|
| Контур.Маркет | Касса 54-ФЗ + товароучёт + ТС ПИоТ + ЕГАИС + Честный знак | S | ts-piot, kkt, markirovka, egais | `kontur-market`, `ts-piot-provider` |
| Контур.ОФД | Передача чеков в ФНС, регистрация кассы | S | kkt, ts-piot | `kontur-ofd` |
| Контур.Маркировка | Полный цикл маркировки (нанесение → выбытие) | S | markirovka | `kontur-markirovka` |
| УЦ / Электронная подпись / Крипто | КЭП для кассы, ЭДО, торгов, МЧД | S | ts-piot, markirovka, zakonodatelstvo | нет — **добить** |
| Контур.Меркурий | Ветсертификация ВетИС (мясо, молоко, рыба) | A | markirovka | нет — **добить** |
| Контур.EDI | Обмен заказами с торговыми сетями | B | markirovka | нет |

### Группа B. Бухгалтерия, налоги, отчётность

| Продукт | Что | Тир | Кластеры | Оффер |
|---|---|---|---|---|
| Контур.Эльба | Бухгалтерия для ИП и малых ООО на УСН | S | zakonodatelstvo | `kontur-elba`, `online-buh`, `bank-elba` |
| Контур.Экстерн | Отчётность в ФНС/СФР/Росстат для бухгалтеров | S | zakonodatelstvo | `kontur-extern` |
| Контур.Зарплата | Расчёт зарплаты и взносов | A | zakonodatelstvo | нет — **добить** |
| Контур.НДС+ | Сверка НДС с контрагентами | A | zakonodatelstvo | нет — **добить** |
| Контур.Норматив | Справочно-правовая база | B | zakonodatelstvo | нет |
| Бухгалтерия / Отчёт.ру / Эксперт | Для бухгалтерских фирм, финанализ | B | zakonodatelstvo | нет |

### Группа C. ЭДО и кадры

| Продукт | Что | Тир | Кластеры | Оффер |
|---|---|---|---|---|
| Контур.Диадок | Юридически значимый ЭДО, приёмка УПД с кодами | S | markirovka, zakonodatelstvo | `kontur-diadoc`, `edo-operator` |
| Диадок: КЭДО | Кадровый ЭДО | S | zakonodatelstvo | `diadoc-kedo` |
| Контур.Доверенность (МЧД) | Машиночитаемая доверенность | S | zakonodatelstvo | `kontur-mchd` |
| Диадок: Логистика | ЭТрН / ГИС ЭПД (140-ФЗ) | A | zakonodatelstvo | `diadoc-logistika` |
| Контур.Документы | Простой ЭДО для малого бизнеса | A | zakonodatelstvo | нет — **добить** |
| Контур.Сайн | Подписание документов с физлицами | B | zakonodatelstvo | нет |

### Группа D. Проверки, риски, продажи

| Продукт | Что | Тир | Кластеры | Оффер |
|---|---|---|---|---|
| Контур.Фокус | Проверка контрагентов | A | zakonodatelstvo | `kontur-focus` |
| Контур.Призма | Проверка по 115-ФЗ (антиотмывание) | A | zakonodatelstvo | нет — **добить** |
| Контур.Компас | Поиск B2B-клиентов, лидген | B | — | нет |
| Закупки / Сопровождение торгов | Госзакупки, тендеры | B | zakonodatelstvo | нет |

### Группа E. Деньги

| Продукт | Что | Тир | Кластеры | Оффер |
|---|---|---|---|---|
| Эльба\|Банк | РКО + бухгалтерия в комплекте | A | zakonodatelstvo | `bank-elba` |
| Контур.Факторинг | Финансирование под уступку | B | zakonodatelstvo | нет |
| Контур.Страхование | Страховые продукты для бизнеса | B | — | нет |

### Группа F. Вне фокуса (тир C — не берём)

Отель, ЖКХ, Недвижимость, Авто, Соцзащита, Реестро, Гособлако,
Налоговый мониторинг, Эгида, Декларант, Толк, Класс, Безопасность,
Школа, Взаиморасчёты. Узкоотраслевое или корпоративное — не наша
аудитория МСБ.

**Итог по охвату:** в витрину фазы 1 идёт ≈18 продуктов (тиры S и A).
Из них 13 уже описаны офферами в `cpa-banners.ts`; добить — 5–6
(ЭП/УЦ, Меркурий, Зарплата, НДС+, Призма, Документы).

## 10. Как это выглядит на сайте

### 10.1 Индекс `/produkty/` — разделы по задаче бизнеса
Группировка не по названиям Контура, а по задаче читателя. Каждый раздел —
заголовок + 2–4 карточки продуктов (визуал из `CPA_BANNERS[].visual`).

```
┌─ /produkty/ ───────────────────────────────────────────────┐
│ eyebrow: Сервисы для бизнеса                                │
│ h1: Подберите сервис Контура под задачу                     │
│ лид + ссылка на /podbor/ (квиз-подбор)                      │
│                                                             │
│ ┌ Касса и маркировка ──────────────────────────────────┐   │
│ │ [Маркет] [ОФД] [Маркировка] [Электронная подпись]    │   │
│ └──────────────────────────────────────────────────────┘   │
│ ┌ Бухгалтерия и отчётность ────────────────────────────┐   │
│ │ [Эльба] [Экстерн] [Зарплата] [НДС+]                  │   │
│ └──────────────────────────────────────────────────────┘   │
│ ┌ Документы и кадры ───────────────────────────────────┐   │
│ │ [Диадок] [КЭДО] [МЧД] [Логистика]                    │   │
│ └──────────────────────────────────────────────────────┘   │
│ ┌ Проверки и деньги ───────────────────────────────────┐   │
│ │ [Фокус] [Призма] [Эльба|Банк]                        │   │
│ └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 Страница продукта `/produkty/<id>/`
```
┌─ /produkty/kontur-market/ ─────────────────────────────────┐
│ Breadcrumbs: Продукты › Касса и маркировка › Маркет        │
│ [визуал МРТ]  h1: Контур.Маркет — касса с ТС ПИоТ          │
│ лид: для кого, ключевая выгода                             │
│ ── Что внутри (буллеты выгод) ──                           │
│ ── Кому подходит ──                                        │
│ ┌──────────────────────────────────────────────────────┐  │
│ │  KonturLeadForm (виджет заявки, тег f74746)          │  │
│ │  триггеры доверия · Реклама · erid: …                │  │
│ └──────────────────────────────────────────────────────┘  │
│ ── Читайте также (статьи кластера ts-piot/markirovka) ──   │
│ ── Сравнить: /sravneniya/kassy/ (F-27) ──                  │
└─────────────────────────────────────────────────────────────┘
```

### 10.3 Бандл под сценарий (в существующих `/scenario/*`)
Блок «что понадобится» — набор продуктов одним экраном под конкретную
задачу. Пример для `/scenario/kofeynya-za-30-dney`:
```
┌─ Что понадобится кофейне ──────────────────────────────────┐
│ 1. Касса + ТС ПИоТ → Контур.Маркет      [Оставить заявку]  │
│ 2. Передача чеков в ФНС → Контур.ОФД    [Оставить заявку]  │
│ 3. Налоги и отчётность → Контур.Эльба   [Оставить заявку]  │
│ 4. Приёмка поставок (УПД) → Диадок      [Оставить заявку]  │
│ Один блок = 4 оффера вместо одного → выше средний чек      │
└─────────────────────────────────────────────────────────────┘
```

### 10.4 Воронка целиком
```
Статья кластера ──┐
/podbor/ (квиз) ──┼─→ /produkty/<id>/ ──→ /sravneniya/ ──→ заявка (виджет)
Сценарий (бандл) ─┘                                          │
                                                   reachGoal в Метрику
```

### 10.5 Связь с навигацией
Пункт «Сервисы» в `NAV_LINKS` (`src/consts.ts`) → `/produkty/`.
Hero главной — вторая кнопка «Подобрать сервис» уже заложена в
`docs/blocks-roadmap.md` (LAB1 #2), ведёт в `/podbor/` → витрину.
