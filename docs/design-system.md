# Дизайн-система Этикетки

Документ описывает токены, компоненты и архитектуру страницы статьи.

---

## Цветовые токены

| CSS-переменная | Hex       | Применение |
|---|---|---|
| `--dark`         | `#111111` | Header нав, footer, тёмные блоки, текст |
| `--pink`         | `#9E2B4F` | Акцент: hover, нумерация FAQ, share-кнопки, CTA-кнопки, eyebrow секций |
| `--lime`         | `#AFCC00` | CTA «Подобрать сервис», badge «Актуально», ЕГАИС-плитка, sup-кнопки |
| `--sand`         | `#EDE8DF` | Плашки-теги, чипсы тем, hover-состояния |
| `--sand-l`       | `#F6F4EE` | **Фон страницы** (body, Header), карточки-фон в `biz-card .bz-tag` |
| `--surface`      | `#F6F4EE` | Алиас `--sand-l` (исторический, использует body) |
| `--line`         | `#E5E1D8` | Границы карточек, разделители секций |
| `--line-2`       | `#D9D4C6` | Пунктирные разделители в 3-колонном списке словаря |
| `--ink-2`        | `#3A3A3A` | Основной текст в prose, описания статей |
| `--ink-3`        | `#666666` | Вторичный текст: лиды, описания категорий, breadcrumbs |
| `--mute`         | `#9A9A9A` | Мета-инфо (даты, время чтения), номера секций |
| `--white`        | `#FFFFFF` | Карточки статей, hero-фон, prose, blog/slovar шапки |

> **Логотип (точка)** — `#E8175D`. Это единственное место, где используется
> исходный ярко-малиновый. Везде в UI: `#9E2B4F` (тёмно-малиновый) и
> `#AFCC00` (приглушённый лайм).

**Фон страницы** теперь `#F6F4EE` (был `#EDE8DF`). Меняли в мае 2026 — мягче,
меньше контраста с белыми карточками, цельнее ощущается под единый дизайн
главной/блога/словаря.

Все токены объявлены в `src/styles/global.css` как CSS-переменные. Старые
литералы `#EDE8DF` встречаются в legacy-местах — постепенно мигрируем
на переменные.

### Категориальные цвета

Источник истины — `CAT_COLORS` в `src/consts.ts`.

| Категория         | Фон (`bg`) | Буква (`letterColor`) | Тег-фон | Тег-текст |
|---|---|---|---|---|
| `ts-piot`         | `#111`     | `#AFCC00` (lime)      | `#111`  | `#fff`    |
| `markirovka`      | `#9E2B4F`  | `#fff`                | `#9E2B4F` | `#fff`  |
| `zakonodatelstvo` | `#AFCC00`  | `#111`                | `#AFCC00` | `#111`  |
| `kkt`             | `#111`     | `#AFCC00` (lime)      | `#111`  | `#fff`    |
| `egais`           | `#111`     | `#AFCC00` (lime)      | `#111`  | `#fff`    |

`bg` — фон большой буквы-аббревиатуры в «Читайте также» и CPA-блоке.
`letterColor` — цвет самой буквы. Используется в: hero статьи, CPA-баннер, карточки «Читайте также».

> **ЕГАИС в правой колонке «Свежее»** — нестандартный случай: фон `--lime`,
> текст `#111`. Сделано в мае 2026 — чтобы тон совпадал с CTA «Подобрать
> сервис» в hero. В тег-плашках статей ЕГАИС остаётся лаймовым с тёмным
> текстом тоже (см. `.tag.t-eg`).

---

## Брендинг

### Логотип

`этикетка<span class="logo-dot">.</span>` — строчные Bebas Neue, точка
после слова — `#E8175D` (исходный pink-логотипа). Раньше было
«ЭТИКЕТКА медиа» (uppercase + sup). Поменяли в мае 2026 — мягче,
плотнее, лучше работает на тёмном футере и светлом шапке.

Используется в `src/components/Header.astro` и `src/components/Footer.astro`.

### Фавикон

`public/favicon.svg` — квадрат 64×64, `border-radius:10`, фон `#111`,
строчная «э» в Bebas Neue лаймом `#AFCC00`, точка `#E8175D` справа
от буквы (как акцент логотипа). Все три брендовых цвета на одной иконке.

---

## Типографика

- **Bebas Neue** — заголовки h1–h3, логотип, uppercase-метки.
  Подключается через Google Fonts CDN в `BaseHead.astro`.
- **Inter** (local woff) — тело текста, UI-подписи.
  Файлы: `public/fonts/inter-*.woff`. Не удалять — нужны при сборке OG.
- **JetBrains Mono** — мета-информация (даты, время чтения, номера
  секций `01/02/03/04`, счётчики). Подключается через Google Fonts
  CDN в `BaseHead.astro`. Внедрено в мае 2026 вместе с редизайном
  главной/блога/словаря.

CSS-переменные шрифтов (для удобства):

```css
--f-display: 'Bebas Neue', Impact, sans-serif;
--f-body:    'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
--f-mono:    'JetBrains Mono', ui-monospace, SFMono-Regular, monospace;
```

---

## Главная (`src/pages/index.astro`)

Редизайн мая 2026. Структура четырёх секций под единой сеткой
`max-width: 1240px`, padding `0 28px`.

| № | Секция | Что внутри |
|---|---|---|
| Hero | Eyebrow + h1 + lead + 3 CTA (Все материалы / Тест готовности / Подобрать сервис) |
| 01 · Свежее | Featured pillar слева (16:10 фото + tag + h3 + лид + «Читать →»), 5 цветных category-cards справа (LAB3 #13 в малом размере): ТС ПИоТ чёрная, Маркировка pink, Законы blue, Кассы purple, **ЕГАИС lime** |
| 02 · Материалы недели | 6 карточек статей 3×2: плейсхолдер 16:9 (radial-градиент по категории + диагональные полосы) или реальное `previewImage`/`heroImage`, tag, h3, дата mono |
| 03 · По формату бизнеса | 6 подборок: Кофейня и общепит (крупная featured, чёрный фон, span:2), Интернет-магазин, Маркетплейс, Самозанятый, Производство, Опт. У каждой: номер, h3, тэги тем (sand-плашки), hint + CTA «Открыть» |
| 04 · Инструменты | 5 интерактивов: Симулятор ТС ПИоТ (pink, span:3), Тест готовности (lime, span:3), Кофейня (blue), ИП→ООО (white), Законы 2026 (dark) |

**Удалено в редизайне:**
- Старая сетка плиток категорий (`.cat-tiles`) — заменена на cat-cards
  внутри «Свежего».
- Старый `tools-strip` — переименован, поправлены пропорции (6 колонок).
- «Серия материалов», «По категориям» (большие плашки подряд), «CTA band»,
  «Mini-newsletter» — рассматривались в прототипе, не пошли в продакшн.

### Плейсхолдеры для отсутствующих изображений

Если у статьи нет `previewImage`/`heroImage`, в карточках главной и
«Материалов недели» отрисовывается стилизованный плейсхолдер `.ph` —
радиальный градиент в цвете категории + наложение из тонких диагональных
полос и radial-затемнения. Цвета:

```css
.ph.p-ts { radial-gradient(#2a2a2a, #0a0a0a) }    /* ТС ПИоТ */
.ph.p-mk { radial-gradient(#b73765, #5b1b30) }    /* Маркировка */
.ph.p-zk { radial-gradient(#2a5d96, #0d2440) }    /* Законы */
.ph.p-kk { radial-gradient(#6e4e92, #2e1f44) }    /* Кассы */
.ph.p-eg { radial-gradient(#c5e324, #7d9300) }    /* ЕГАИС */
```

`.ph:has(img)` отключает декор — при наличии реального изображения
оно показывается без перекрытий.

---

## Все материалы (`src/pages/blog/index.astro`)

Шапка-секция с белым фоном, breadcrumbs, eyebrow «Архив», h1 «Все материалы»,
лид и поисковый инпут с кнопкой «Найти». Дальше — `BlogFilter` (React)
без изменений.

---

## Словарь (`src/pages/slovar/index.astro`)

Полная переработка мая 2026. Структура:

1. **Шапка** (белый фон): breadcrumbs, eyebrow «Справочник», h1 + лид,
   поисковый инпут с лупой-SVG.
2. **Плитки А-Я** — сетка `repeat(16, 1fr)`, квадратики с буквами.
   Активная буква — `#111` с lime-буквой. Буквы без терминов — серые,
   `pointer-events: none`. Клик меняет активный letter-block.
3. **Активный letter-block** (один за раз): большая малиновая буква +
   «Термины на букву "Х"», под ней 3-колонный список терминов с
   пунктирными разделителями.

Каждый термин: жирный pink-name + " — " + расшифровка + (если есть)
mono-ярлык категории, ведущий на `/category/{slug}/`.

При вводе в поиск все буквы раскрываются, не совпадающие термины
скрываются (`.search-active`).

---

## Страница статьи (`BlogPost.astro`)

### Hero — сетка 58/42

```
┌─────────────────────┬──────────────────┐
│  58fr               │  42fr            │
│  Тёмная зона        │  Песочная зона   │
│                     │                  │
│  [cat badge]        │  breadcrumbs     │
│                     │  badges row      │
│  большая буква      │  H1              │
│  (opacity 0.15)     │  ─────────────   │
│                     │  lead (italic)   │
└─────────────────────┴──────────────────┘
height: calc(100vh - 56px), min-height: 480px
```

**Правая колонка (justify-content: space-between):**

1. `nav.art-hero-breadcrumbs` — Главная / Статьи / [категория]
2. `.art-hero-top` — badges row:
   - `span.art-updated-badge` (lime) — «Актуально на [updatedDate]» (только при наличии `updatedDate`)
   - `span.art-hero-readtime` — время чтения (серый)
3. `h1.art-h1` — заголовок (Bebas Neue, 3.2rem)
4. `.art-hero-bottom` (border-top: 2px solid #111) — `p.art-lead` (description)

### Body — сетка 28/72

```
┌────────────┬────────────────────────────┐
│  28fr      │  72fr                      │
│  Sidebar   │  Content                   │
│            │                            │
│  sticky    │  TOC → prose →             │
│  CPA-      │  callouts/checklist/FAQ →  │
│  баннер    │  share →                   │
│            │  CPA-врезка →              │
│            │  «Читайте также»           │
└────────────┴────────────────────────────┘
```

**Sidebar:** `position: sticky; top: calc(56px + 16px)`. Скрывается при ≤1024px.

### Блоки контентной колонки

| Блок | Условие | Описание |
|---|---|---|
| `toc` (`<details>`) | ≥3 заголовков H2/H3 | Оглавление, разворачивается по клику |
| `.prose` | всегда | `<slot/>` — MDX-контент статьи |
| `art-share` | всегда | Telegram + ВКонтакте; URL из canonical |
| `.cpa-block` | всегда | CPA/партнёрский баннер (из `src/data/cpa-banners.ts`) |
| `.related` | ≥2 совпадений | «Читайте также» — адаптивная сетка |
| `art-foot` | всегда | Правовая оговорка |

**Удалены** (не используются): `art-tags`, `inline-subscribe`, `helpful`,
`post-nav` (пред./след. статья), `art-cta` («Остались вопросы?»).

### CPA-блок

Размещается между `art-share` и «Читайте также». Данные — из `src/data/cpa-banners.ts`.

Структура HTML:
```html
<div class="cpa-block">
  <div class="cpa-visual"><!-- abbrev, категориальный фон --></div>
  <div class="cpa-body">
    <span class="cpa-eyebrow"><!-- eyebrow --></span>
    <strong class="cpa-title"><!-- title --></strong>
    <p class="cpa-desc"><!-- description --></p>
  </div>
  <div class="cpa-action">
    <a class="cpa-btn" href="..."><!-- cta --></a>
  </div>
</div>
```

Выбор баннера (в порядке приоритета):
1. `cpa:` в frontmatter статьи
2. `CATEGORY_DEFAULT_CPA[primaryCategory]`
3. `'default-ts-piot'` (запасной)

### «Читайте также» — адаптивные макеты

Берётся до 4 постов (scoring: теги ×2, категории ×1). Макет определяется
детерминированно — без `Math.random()`, иначе SSG не работает.

| Постов | Макет | Логика выбора |
|---|---|---|
| 0–1 | нет блока | скрыт |
| 2 | **A2** — два равных столбца | всегда |
| 3 | **A3** или **B** | `slugCharSum % 2 === 0` → A3, иначе B |
| 4+ | **C** — 1 большая + 3 компактных | всегда |

`slugCharSum` — сумма char codes символов slug текущей статьи. Постоянна для
одной статьи, варьируется между статьями.

**Макет A2** — 2 равных столбца `1fr 1fr`.

**Макет A3** — 2 столбца, левый шире: `3fr 2fr`. Левая карточка крупная
(заголовок 1.4rem, описание), правая — стандартная.

**Макет B** — 2 столбца `2fr 3fr`. Правая карточка крупная.

**Макет C** — grid `3fr 2fr`, первая строка занимает весь левый столбец (`grid-row: span 2`).
Три компактных справа.

### Прогресс-бар и «наверх»

- `#rp-bar` — `position: fixed; top: 0; height: 3px; background: #E8175D`
- `#back-to-top` — `position: fixed; bottom: 24px; right: 24px`; появляется при `scrollY > 400`

### Брейкпойнты

| Ширина   | Изменение |
|---|---|
| `≤1024px` | Sidebar скрыт, контент 100% |
| `≤820px`  | Hero стэкируется (1fr), hero-img: height 280px |
| `≤560px`  | h1: 2rem, уменьшение паддингов |

---

## Компоненты

### `Callout.astro`

Акцентная врезка для выделения ключевых мыслей в теле статьи.

```astro
import Callout from '../../components/Callout.astro';

{/* Тёмный (по умолчанию): чёрный фон, лаймовый акцент */}
<Callout>
  Если статус кода не позволяет продажу, касса обязана заблокировать операцию.
</Callout>

{/* Светлый: песочный фон, розовый акцент */}
<Callout variant="light">
  При повторном нарушении должностное лицо могут дисквалифицировать на срок до двух лет.
</Callout>
```

- `variant?: 'dark' | 'light'` (default: `'dark'`)
- `**жирный**` и `*курсив*` внутри получают цвет акцента
- Декоративная `"` через `::before` (opacity 0.18)

### `FAQ.astro`

```astro
import FAQ from '../../components/FAQ.astro';
<FAQ
  title="Часто задаваемые вопросы"   // опционально
  items={[
    { q: 'Вопрос?', a: 'Ответ.' },
  ]}
/>
```

- Автоматически эмитирует `<script type="application/ld+json">` с FAQPage schema
- `<details name="faq">` — нативный аккордеон (открыт только один элемент)
- Нумерация розовым (Bebas Neue), стрелка-индикатор с CSS transition
- Яндекс и Google показывают FAQ прямо в выдаче при наличии FAQPage schema
- Компонент **необязателен** — не добавляйте его ради галочки. Правила выбора: [content-types.md → FAQ в статье](content-types.md#faq-в-статье)

### `Checklist.astro`

```astro
import Checklist from '../../components/Checklist.astro';
<Checklist
  id="уникальный-id"     // обязателен — ключ для localStorage
  title="Что проверить"  // опционально
  items={[
    'Пункт 1',
    'Пункт 2',
  ]}
/>
```

- Состояние хранится в `localStorage` с ключом `checklist_<id>`
- Прогресс-счётчик «X / N выполнено» обновляется в реальном времени
- Кнопка «Сбросить» очищает localStorage и снимает все галочки
- Выполненные пункты: зачёркивание + opacity 0.5

---

## Страница редакции (`/about/avtor/`)

- SVG-аватар с инициалами «ЭМ» (тёмный фон `#111`, лаймовый текст `#C8F500`, Bebas Neue)
- Карточка автора: тёмный блок, имя / роль / email
- Контактная форма: имя + email + textarea + submit
- `action="mailto:hello@etiketka-media.ru" method="get"` — клиентская, без бэкенда

---

## Источники истины

| Данные | Файл | Ключ |
|---|---|---|
| Заголовок, описание, URL сайта | `src/consts.ts` | `SITE_TITLE`, `SITE_URL` |
| Навигация | `src/consts.ts` | `NAV_LINKS` |
| Категории (slug, название) | `src/consts.ts` | `CATEGORIES` |
| Sidebar баннер | `src/consts.ts` | `SIDEBAR_BANNER` |
| CPA/партнёрские баннеры | `src/data/cpa-banners.ts` | `CPA_BANNERS`, `CATEGORY_DEFAULT_CPA` |
| Сценарии штрафов | `src/data/penalties.ts` | `SCENARIOS` |

---

## Changelog

### 2026-05-26 — Редизайн главной, /blog/, словаря

#### Новые токены и шрифт

| Что | Где | Зачем |
|---|---|---|
| `--surface: #F6F4EE` | `src/styles/global.css` | Новый фон страницы. Раньше body был на `--sand: #EDE8DF` — слишком тёплый, ел контраст белых карточек |
| `--sand-l: #F6F4EE` | `src/styles/global.css` | Алиас surface для семантики «light sand» |
| `--ink-2: #3A3A3A`, `--ink-3: #666`, `--mute: #9A9A9A` | `src/styles/global.css` | Шкала второстепенного текста, заменяет россыпь `#666`, `#888`, `#aaa` |
| `--line: #E5E1D8`, `--line-2: #D9D4C6` | `src/styles/global.css` | Границы карточек и пунктирные разделители — мягче, чем `rgba(0,0,0,.12)` |
| `--f-display`, `--f-body`, `--f-mono` | `src/styles/global.css` | Удобные алиасы для шрифтов |
| JetBrains Mono | `src/components/BaseHead.astro` | Третий шрифт — мета-информация (даты, время чтения, номера секций) |

#### Брендинг

| Что | Было | Стало |
|---|---|---|
| Логотип | `ЭТИКЕТКА<sup>медиа</sup>` (uppercase) | `этикетка<span class="logo-dot">.</span>` (lowercase + pink-точка) |
| Файлы | `Header.astro`, `Footer.astro` — оба обновлены |
| Фавикон | Сине-синий градиент с буквой «Э» | Чёрный квадрат, лаймовая «э» в Bebas Neue, pink-точка справа. Три брендовых цвета на одной иконке. `public/favicon.svg` |

#### Главная (`src/pages/index.astro`)

Переписана целиком. Удалено: старая `.cat-tiles` сетка плиток, старый
`Latest posts` (1 featured + 6 равных карточек). Добавлено:

| Секция | Что |
|---|---|
| Hero | Без правой колонки с анонсами. Eyebrow + h1 + лид + 3 CTA |
| 01 · Свежее | Последний pillar слева (16:10 фото) + 5 категорий справа цветными мини-плашками |
| 02 · Материалы недели | 6 карточек 3×2 с плейсхолдерами по категории (radial-градиент + диагональные полосы) или с реальными `previewImage` |
| 03 · По формату бизнеса | 6 подборок: Кофейня крупная featured (чёрная), 5 обычных |
| 04 · Инструменты | 5 интерактивов (Симулятор, Тест, 2 сценария, Трекер 2026) |

Категория ЕГАИС в `.cat-card` — лаймовый фон с тёмным текстом (вместо
зелёного), чтобы совпадал с CTA «Подобрать сервис» в hero.

#### Все материалы (`src/pages/blog/index.astro`)

Переписана только шапка-секция (breadcrumbs, eyebrow «Архив», h1, лид,
поисковая форма) под единый стиль. `BlogFilter.tsx` оставлен без
изменений — переключатель режимов отображения карточек (Карточки / Фото
/ Дайджест) рассматривался в прототипе, в продакшн не пошёл.

#### Словарь (`src/pages/slovar/index.astro`)

Полная переработка. Удалено: линейный алфавитный индекс с большими
буквами сбоку. Добавлено:

- Плитки А-Я (33 квадратика, `repeat(16, 1fr)`). Активная буква — `#111`
  с lime-буквой. Буквы без терминов — `pointer-events: none`, `opacity: .5`.
- Один активный letter-block за раз: большая pink-буква + h2 + 3-колонный
  список терминов (`column-count: 3`).
- Каждый термин: pink-name + `" — "` + расшифровка + (если есть) mono-ярлык
  категории со ссылкой на `/category/{slug}/`.
- Поиск без перезагрузки: класс `.search-active` раскрывает все буквы,
  не совпадающие термины получают `.hide`.

Скрытие/показ — нативный JS в `<script>` блоке страницы, без React.

#### Header и Footer

| Что | Было | Стало |
|---|---|---|
| `max-width` шапки/футера | 900px | 1240px (под ширину main на главной) |
| Фон Header | `#EDE8DF` (sand) | `var(--surface)` (sand-l) — тот же, что body, без шва |
| Граница Header | `rgba(0,0,0,.1)` | `var(--line)` |
| Высота Header | 56px | 64px |

#### Что осталось вне редизайна

- Шаблон статьи (`src/layouts/BlogPost.astro`) — не трогался, прототип
  «Статья» из обсуждения был хуже текущего.
- `/podbor/` как отдельный раздел не вводили — в прототипе тоже признали
  хуже текущих квизов.
- /category/, /instrumenty/, /dashboard/, калькуляторы, сценарии,
  тренажёр ЭДО — без изменений.

---

### 2026-05-03 — Упрощение статьи, адаптивный related, CPA-блок

#### Новые компоненты и данные

| Файл | Что |
|---|---|
| `src/components/Callout.astro` | Акцентная врезка (dark/light), декоративная `"` |
| `src/data/cpa-banners.ts` | Единый источник CPA/партнёрских баннеров |

#### `src/layouts/BlogPost.astro`

**Удалено:**

| Элемент | Причина |
|---|---|
| `art-tags` (пилюли тегов) | Избыточно — теги доступны через `/tags/` |
| `inline-subscribe` (врезка с формой подписки) | Перегружает статью |
| `helpful` («Была ли полезна?») | Нет бэкенда, данные нигде не используются |
| `post-nav` (пред./след. статья) | Низкий CTR, занимает место |
| `art-cta` («Остались вопросы?» + mailto) | Заменён CPA-блоком |

**Добавлено / изменено:**

| Элемент | Стало |
|---|---|
| CPA-блок | Баннер из `src/data/cpa-banners.ts` после `art-share` |
| «Читайте также» | До 4 постов; 4 адаптивных макета (A2/A3/B/C) |
| Hero meta | Только `art-updated-badge` + `art-hero-readtime`; убраны `art-pubdate`, `art-verified-badge`, `art-hero-hash` |
| `content.config.ts` | Добавлено поле `cpa: z.string().optional()` в схему blog |

#### Предыдущий редизайн (2026-05-02)

Добавлены: `FAQ.astro`, `Checklist.astro`, hero 58/42, sidebar sticky,
share-кнопки, прогресс-бар, кнопка «наверх», замена CSS-переменных на hex-токены.
Обновлена страница редакции `/about/avtor/`.
