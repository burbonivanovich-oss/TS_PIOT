---
title: Бэклог — идеи и улучшения сайта
createdDate: "2026-05-05"
lastModified: "2026-05-19"
type: reference
status: draft
---

# Бэклог

Единое место для идей, задач и улучшений, которые не вошли в текущий спринт.
Статусы: `idea` → `planned` → `in progress` → `done`.
Приоритеты: **P0** — блокер, **P1** — важно, **P2** — хорошо иметь.

---

## 🛠 Инструментарий контент-операций

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 37 | Подключить Wordstat API через `oauth.yandex.ru` (без рекламного кабинета). **Сделано:** два контура — точечный (`fetch.mjs` по `seo.keywords` + content-plan, ~240 ключей, поля `shows`/`topShows`/`trend`/`history`) и trend discovery (`discover.mjs` + `diff-snapshots.mjs` по 162 широким seed-ам с weekly diff NEW/RISING/FALLING/DROPPED). Workflow `Wordstat Weekly Refresh` запускается еженедельно. Интегрировано в `/find-topics`, `/cluster-gaps`, `/maintain-content`. См. `docs/wordstat.md`. | P0 | done |
| 51 | После второго weekly-прогона (когда появится первый осмысленный diff) — прогнать `/find-topics` на нём, проверить что скилл корректно подхватывает NEW/RISING и предлагает темы. **Blocked:** сейчас выполнен 1 прогон (2026-05-18), нужен второй для появления diff. После следующего weekly запуска `wordstat-weekly.yml` задача готова к выполнению. | P1 | blocked |
| 52 | Ревизия `seeds.json` после 2–3 прогонов: выкинуть seeds, у которых всегда 0 фраз в выгрузке (слишком узкие), добавить недостающие если в diff проявятся «сироты» (фразы без подходящего seed). **Blocked:** ждём 2–3 weekly прогонов. | P2 | blocked |
| 53 | Уборка orphaned ключей в `keys.json`: записи, которых уже нет в `.candidates.json` 90+ дней, удалять. **Сделано:** `scripts/wordstat/cleanup-orphans.mjs` — dry-run по умолчанию, `--apply` для удаления, `--age N` для порога. Идемпотентен. Сейчас orphans = 0 (single прогон, все 299 фраз в активных). Скрипт сработает когда статьи начнут редактироваться/удаляться. | P2 | done |
| 54 | Расширить `/maintain-content`: использовать `history` из `keys.json` для трендового анализа. **Сделано:** Шаг 2а-bis детализирован — формула `recent3 / baseline6` (9-месячное окно), пороги ≥2 → 📈 «обновить», ≤0.5 → 📉 «архив/объединить». Дополнительно — сравнение `shows` vs `topShows`: если topShows ≥ 3×shows, формулировка `seo.keywords` слишком узкая, в отчёте предлагается переформулировка на `topPhrase`. | P1 | done |

> **Связь:** п. 37 пересекается с пп. 6–11 (аналитика).

---

## 🔍 Фактчек и верификация

**Состояние на 2026-05-18:** написан свой стек проверки фактов вместо
плагина claude-blog (#40 done). Пилотный прогон на статье о ТС ПИоТ
нашёл 3 критических расхождения, статья отредактирована (#39 in progress,
1/83). Редполитика для решений зафиксирована в `docs/editorial-policy.md`
(классы A/B/C). Все скиллы пайплайна (`/create-article`, `/new-post`,
`/publish-article`, `/maintain-content`) обновлены — ссылки на старый
`/blog factcheck` заменены на наш `/factcheck`.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 38 | Установить плагин `claude-blog` в `~/.claude/skills/`. **Не делаем:** в облачном эфемерном контейнере плагин не выживает между сессиями, а под нашу специфику (НПА РФ, штрафы, сроки маркировки) написан свой стек — см. п. 40. | P0 | won't fix |
| 39 | Backfill фактчека на 80 статей блога без маркера. **Сделано:** **83/83 = 100% покрытие** (+75 за сессию через 18 параллельных батчей). За сессию: **12 фейковых нормативных актов** (ПП № 257, № 2456, № 2457, № 2464, № 815 отменён, № 1951, № 1862; ФЗ № 421 вместо 420, № 263 вместо 429 повторён 2×, № 204 от 04.08.2023 повторён 2× в обзорах касс; приказ ФСРАР № 397 ложная атрибуция). + Новый паттерн: реальный номер ПП в неправильной тематике (ПП № 2099 как ТС ПИоТ вместо молочки). + 70+ системных ошибок: НДС 22%, лимиты УСН 490.5 / ПСН 20, порог НДС-на-УСН 20 млн, ставка НПД на WB 6%, форма 8 деклараций алкоголя отменена с 15.01.2025, ФСРАР → РАТК, дата 289-ФЗ, МРОТ 2025/2026, ст. 15.12 ч. 2 КоАП, «крепкий алкоголь >9%», маркировка кег с 01.04.2023 и др. Эти случаи закрыты #61/#31 на пайплайн-уровне; новые статьи через `/create-article` не должны их пропускать. | P0 | done |
| 40 | Внешняя верификация claims через WebFetch/WebSearch к первоисточникам. **Сделано:** свой стек `scripts/factcheck/extract-claims.mjs` (regex-парсинг дат, сумм, ст. КоАП, ссылок) + каталог источников `src/data/factcheck/sources.json` (consultant.ru / pravo.gov.ru блокируют WebFetch → используем WebSearch) + скилл `/factcheck <slug>` + формат отчёта в `src/data/factcheck/results/<slug>.json`. | P0 | done |
| 41 | Quarterly re-factcheck: автоматизация в `/maintain-content`. **Сделано:** Шаг 1 теперь добавляет в очередь статьи у которых маркер `.claude/factchecked/<slug>` старше 90 дней или отсутствует; Шаг 2а заменён на запуск `/factcheck <slug>` со чтением `results/<slug>.json` для принятия решения; reviewDate переключён на +3 месяца (синхрон с quarterly циклом). | P1 | done |
| 55 | Распараллелить `/factcheck` через subagent dispatch — батч из 10–15 статей за один прогон. **Сделано:** `.claude/commands/factcheck-batch.md` — параметры `--count`, `--filter`, группировка по теме для переиспользования whitelist'а агентом, шаблоны промптов для 7 тематических кластеров. Тайминг: 16 статей за ~15 минут (×6 ускорение vs последовательный). | P1 | done |
| 56 | Расширить `extract-claims` под нестандартные паттерны: «пятнадцать тысяч рублей» (числа словами), «с начала года» (контекстные даты), упоминания пунктов без «ст.». **Сделано:** добавлены 3 новых типа claims — `MONEY_WORDS`, `DATE_CONTEXT`, `NPA_PUNKT`. Inventory по всем 83 статьям: +39 NPA_PUNKT (явные пункты без статьи), +5 DATE_CONTEXT (контекстные даты типа «с начала года»), +0 MONEY_WORDS (наши статьи пишут цифрами — регекс на подстраховку). | P2 | done |
| 57 | Pre-commit hook: блокировать `git commit` статьи с `draft: false`, если нет маркера `.claude/factchecked/<slug>`. **Сделано:** `scripts/git-hooks/pre-commit-factcheck-guard.mjs` — проверяет staged blog-файлы, exit 1 с понятным сообщением. `scripts/git-hooks/install.sh` устанавливает через `git config core.hooksPath` (без husky). Bypass: `git commit --no-verify`. | P1 | done |
| 61 | research-specialist обязателен перед writer для тем с НПА. **Сделано:** (1) гейт в `/create-article` Стадия 1 — для категорий с НПА research-specialist обязателен; (2) schema brief'а с `frontmatter.sources[].verified=true`; (3) whitelist НПА в `sources.json.npaWhitelist` (11 ФЗ + 11 ПП + 3 Приказа + ст. КоАП/УК); (4) extract-claims расширен типами NPA_UK / NPA_NK / NPA_PP_NUMBERED / NPA_PRIKAZ; (5) `scripts/factcheck/audit-npa-references.mjs` — регрессионный аудит, флаг `--strict` для CI; (6) Стадия 3 factcheck теперь включает прогон audit-npa --strict до /factcheck. Первый прогон выявил 37 незнакомых ФЗ + 18 незнакомых ПП — материал на отдельный цикл верификации. GitHub Issue #31 closed. | P0 | done |
| 58 | Приоритет backfill #39: начать с риск-зоны — ТС ПИоТ (3 статьи), штрафы и маркировка (~10 статей), затем налоговые сроки 2026 (~5 статей). **Сделано:** благодаря приоритизации к 100% покрытию мы пришли через серию из 18 параллельных батчей. Риск-зоны (ТС ПИоТ, штрафы маркировки, налоги 2026) проверены первыми, обзоры моделей и нерегулируемые темы — последними. | P0 | done |

---

## 🎯 Точность определения дубликатов и пробелов

Сейчас `/cluster-gaps` и `/find-topics` определяют каннибализацию эвристически по `frontmatter.title + seo.keywords + categories` с порогом «>50% / >60% пересечения». Это субъективно и шумно: легитимные апдейты режутся, реальные дубли пропускаются.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 42 | Embeddings-based similarity для каннибализации. **Сделано:** `scripts/audit/embed-articles.mjs` (Jina v3 multilingual 1024d / OpenAI 3-small 1536d, pluggable провайдер) + `scripts/audit/similarity.mjs` (cosine, top-6 соседей, пороги 0.92 — дубль / 0.80 — близкие). Workflow `embeddings-monthly.yml` 1-го числа месяца обновляет матрицу — нужен `JINA_API_KEY` в secrets (бесплатно 1M токенов/мес) либо `OPENAI_API_KEY`. `/find-topics` Шаг 3 теперь читает `similarity.json` как второй уровень каннибализации поверх лексического. | P1 | done |
| 43 | Альтернатива п. 42 (простая): TF-IDF индекс ключей всех статей, пересчитывать пересечение точнее текущей эвристики | P2 | idea |
| 44 | Пересмотреть порог «дубль >60%» в `/monitor-rss`. **Сделано:** Шаг 4 разделён на 3 порога: ≥ 90% → «обновить [slug]», 60–90% → «возможный апдейт — нужна ручная оценка» (новый блок в отчёте), < 60% → «новая статья». Не отбраковываем 60–90% автоматически — решение остаётся за человеком. | P1 | done |

---

## 📡 Мониторинг и фильтрация источников

Сейчас `/monitor-rss` использует жёсткое окно 14 дней и одинаково фильтрует методички, аналитику конкурентов и НПА. Часть полезных сигналов теряется.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 45 | Конфигурируемое окно мониторинга: `--window 14d` (срочно публиковать), `--window 30d` или `60d` (для планирования). **Сделано:** аргумент `--window 14d|30d|60d|90d` в `/monitor-rss`. Размер окна задаёт горизонт принимаемых материалов. | P1 | done |
| 46 | Отдельный поток «методички и разъяснения без дедлайна» → автоматически в `/maintain-content` как кандидаты на обновление существующих статей, не в `/plan-content` как новые темы. **Сделано:** аргумент `--type news|guidelines|all` в `/monitor-rss`. Шаг 3 классифицирует материалы по 8 признакам, news → `/plan-content`, guidelines → `src/content/wiki/maintain-queue.md`, читается `/maintain-content` Шагом 1а как приоритетный список. | P1 | done |
| 47 | Отдельный отчёт «о чём пишут конкуренты» из аналитики агрегаторов (Клерк, Бухонлайн) — как trend detector, не как источник тем дословно. Не смешивать с НПА. **Сделано:** скилл `/monitor-competitors`. Источники — только агрегаторы и аналитика (НПА — у `/monitor-rss`). Шаг 4 применяет пороги дублей. Шаг 5 выделяет горячие темы (≥ 5 материалов), кейсы для формата #33, методологические дискуссии. Журнал — `src/content/wiki/competitor-trends.md`. Главное правило: не копировать темы дословно. | P2 | done |

---

## 🚀 Темп публикаций и автоматизация пайплайна

Сейчас темп — 2 опорных + 4–6 сателлитов в месяц. Чтобы поднять до 4 опорных + 8 сателлитов и одновременно увеличить P0-лимит с 3 до 5–6, нужна автоматизация ручных шагов. Без этого рост темпа = деградация качества.

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 48 | Автоматизировать перелинковку: новый шаг в `/create-article`. **Сделано:** добавлена Стадия 2а — после `content-writer` пересборка `linkgraph.mjs` + проверка исходящих ссылок новой статьи (топ-5 кандидатов из score) и входящих (поиск релевантных сирот/слабых). Шлюз 2а требует ≥ 3 исходящих + ≥ 1 входящая до следующей стадии. | P1 | done |
| 49 | Сделать `/blog analyze` обязательным gate перед `/release-article` — оценка 0–100 по 6 категориям как блокер. **Сделано:** свой `/analyze-article <slug>` (плагин claude-blog в облачном контейнере не выживает). Шкала 100 по 6 категориям (качество текста 20, SEO 20, E-E-A-T+factcheck 20, контент-граф 20, техника 20, AI Citation бонус 10). `blocker: true` если балл < 70 / маркер factcheck > 180 дней / audit-npa-references --strict упал / check-blog-links упал. `/release-article` Шаг 1 теперь обязательно вызывает gate. | P1 | done |
| 50 | После закрытия пп. 38–40 и 48–49 — поднять P0-лимит в `/plan-content` с 3 до 5 тем за цикл, расширить окно дедлайнов с 60 до 90 дней. **Сделано (мягкий вариант):** окно расширено 60→90 дней (default). P0-лимит остался 3, добавлен override через флаг `--p0-limit <N>` для экстренных случаев (всплеск НПА, конец квартала, пилот #33). После 2 циклов работы на повышенном лимите без падения балла `/analyze-article` ниже 75 — поднять default до 5. | P2 | done |

---

## 📊 Аналитика и данные

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 6 | Подключить Google Search Console API — позиции и клики по статьям | P1 | idea |
| 7 | Подключить Яндекс.Вебмастер API — позиции и индексация | P1 | idea |
| 8 | Подключить Яндекс.Метрика API — трафик по URL | P1 | idea |
| 9 | Интегрировать аналитику в `/maintain-content` — показывать позиции при аудите | P1 | idea |
| 10 | Настроить `/blog google` (плагин claude-blog) — GSC + GA4 | P2 | idea |
| 11 | Дашборд: сводная таблица статей с позицией + трафиком + оценкой | P2 | idea |
| 69 | **Hero-backfill safety-net.** Корневая причина пропуска hero — batch-коммиты статей сразу с `draft: false` обходят `auto-publish.yml`, который генерит hero только при flip `true → false`. **Сделано:** новый workflow `.github/workflows/hero-backfill-daily.yml` крутится cron'ом раз в сутки (21:00 UTC = 00:00 МСК), находит статьи без `heroImage` и догенеряет до 10 за прогон. Контроль расходов: max $0.125/день. В обычный день — no-op. Дополнительно: вариант 1 (немедленная догенерация 38 текущих статей) запускается вручную через Generate Hero Images → Run workflow с пустым slug. | P0 | done |
| 70 | **Цели в Метрике через Management API.** **Сделано 2026-05-19:** декларативный `src/data/metrika/goals.json` (25 целей: 9 шагов воронки флагмана + 4 части DataMatrix + 5 статусов кода + 5 интеракций + 2 калькулятора + zakon-2026), идемпотентный `scripts/metrika/sync-goals.mjs` (создаёт недостающие, обновляет имена, не удаляет orphan), workflow `metrika-sync-goals.yml` с DRY_RUN-default + автопуш на изменения `goals.json`, документация `docs/metrika.md`. **Что осталось от человека:** прописать `METRIKA_OAUTH_TOKEN` в секреты GitHub (OAuth с oauth.yandex.ru, scope `metrika:write`) и запустить workflow один раз в DRY_RUN, потом в боевом. Клиентскую отправку (`src/utils/track.ts`) это не заменяет. | P2 | done |
| 68 | **Подумать над мини-админкой.** Не «делаем сразу», а исследование: что туда могло бы попасть и нужна ли она вообще. Сейчас часть рутины делается через GitHub Actions UI (Generate Hero Images, Wordstat Weekly Refresh), часть через slash-команды в Claude, часть через ручное чтение JSON-отчётов в `src/data/`. Возможные кандидаты в админку: (1) дашборд статей со статусами factcheck/heroImage/балл analyze/трафик — фактически #11; (2) триггеры workflow'ов одной кнопкой вместо UI Actions; (3) очередь `maintain-queue.md` визуально; (4) preview JSON-отчётов (linkgraph, similarity, npa-audit, factcheck/results); (5) toggle draft/published; (6) форма для быстрого добавления RSS-источника. **Сначала ответить на вопросы:** что из этого делается чаще всего и реально болит? Где админка даст профит vs текущий путь? Авторизация (GitHub Pages статика — auth нетривиальна; cloudflare/vercel с middleware vs простой password в localStorage)? Где живёт — в `/admin/` на etiketka.media с `noindex` или отдельный поддомен? После ответов — сформировать MVP (3-5 экранов) и завести как отдельные задачи. **Повторно поднято 2026-05-19** — мотив «какие-то моменты можно сделать нагляднее»; потребность не разовая, пора переходить от research к MVP-скоупу. | P1 | research |

---

## ⚙️ Технические задачи

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 14 | Pagefind: добавить фильтры по категории в поиске. **Сделано:** BlogPost.astro получил `data-pagefind-filter="type:Статья,category:<имя>"`, slovar — `type:Словарь`, pillar — `type:Раздел`. В `search.astro` PagefindUI с `showFilters: true, filtersSidebar: true`. | P2 | done |
| 15 | Pagefind: добавить pillar-страницы и глоссарий в индекс. **Сделано (+ найден баг):** тело pillar-страниц вообще не рендерилось на сайте — 46 ссылок из #24 были невидимы и для пользователей, и для Pagefind. В `category/[category].astro` добавлен render() pillar.body через `<PillarContent />` с обёрткой `<section class="pillar-body prose" data-pagefind-body>`. Стилизация. Glossary получил `data-pagefind-meta="type:glossary"`. | P2 | done |
| 16 | Добавить время чтения в шапку статьи. **Сделано:** уже было реализовано — `art-hero-readtime` в BlogPost.astro показывает `{readingTime} мин` при наличии параметра. | P2 | done |
| 17 | Оглавление (TOC) для длинных статей (> 2000 слов). **Сделано:** условие уточнено — TOC показывается при `≥ 4 H2/H3` ИЛИ `wordCount ≥ 2000 и ≥ 2 заголовка`. Для длинных статей открыт по умолчанию. | P2 | done |
| 18 | RSS-автопостинг в Telegram-канал при публикации | P2 | idea |
| 19 | Подключить Open Graph для Telegram (og:image через Satori уже работает) — протестировать. **Сделано:** проблема была в default `/og-default.svg` (Telegram не читает SVG). Создан `public/og-default.png` (1200×630), `BaseHead.astro` дефолт → PNG. Добавлены `og:image:secure_url`, `og:image:type`, `og:image:alt`. Чек-лист и инструкция проверки через @WebpageBot — `docs/og-telegram.md`. | P1 | done |

---

## 📝 Контент

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 22 | Добавить `reviewDate` в статьи, у которых его нет. **Сделано:** `scripts/audit/set-review-dates.mjs` проставил `reviewDate = pubDate + 90 дней` в 67/83 статьях (синхрон с quarterly re-factcheck циклом). Идемпотентен. | P1 | done |
| 23 | Аудит внутренней перелинковки: найти статьи-сироты. **Сделано:** `scripts/audit/linkgraph.mjs` строит граф `slug → outbound[] / inbound[]`, находит orphans (0 входящих), weak (1 входящая), deadends (0 исходящих); для каждой сироты предлагает 3 кандидата для ссылки по scoring «категории ×2 + теги ×1». Отчёт в `src/data/audit/linkgraph.json`. **Первый прогон вскрыл: 61/83 статей — сироты, 10 тупиков.** Это огромная SEO-проблема, для закрытия — отдельная задача #59 (применить предложения). | P1 | done |
| 59 | Применить предложения linkgraph: добавить минимум по 1 входящей ссылке из топ-кандидата каждой сироте. **Сделано:** обработка тремя параллельными subagent'ами по категориям (markirovka 12, kkt+ts-piot 7, zakonodatelstvo+egais 15). Большая часть сирот закрылась побочно через `fix-broken-blog-links` (#60), остальное — точечные вставки в донорах. **Сирот: 61 → 34 (после #60) → 0.** Weak (1 входящая) выросло с 13 до 46 — это нормально, теперь надо думать про усиление через #24 (обновление pillar-страниц). | P1 | done |
| 60 | Битые внутренние ссылки `/blog/<slug-без-даты>/` (формат не соответствует `post.id` Astro glob loader). **Сделано:** 196 ссылок в 59 файлах исправлены через `scripts/audit/fix-broken-blog-links.mjs` (идемпотентен), 1 — вручную (`psn-nds-2026` → `nalogi-msb-2026`). Регрессионный чек `scripts/audit/check-blog-links.mjs`. Сирот в linkgraph: 61 → 34. GitHub Issue #30. | P0 | done |
| 24 | Обновить pillar-страницы: добавить ссылки на новые статьи. **Сделано:** в каждом из 5 pillars добавлен раздел «Связанные материалы» с подгруппами по теме. 46 weak-статей (1 входящая) получили ссылку из своего pillar. До правок pillars не ссылались на блог вообще. Распределение: ts-piot 4, markirovka 12, zakonodatelstvo 17, kkt 8, egais 5. | P2 | done |

---

## 🎨 UX и дизайн

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 25b | Визуальная проверка мобильной вёрстки в браузере (iPhone SE/14, Android, iPad) — после code-аудита и фиксов таблиц/padding | P2 | idea |
| 28 | Кнопка «Поделиться» (Telegram, VK) без внешних трекеров | P2 | idea |

---

## 🔌 Интеграции и дистрибуция

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 29 | Завести Telegram-канал и опубликовать первые посты | P1 | idea |
| 30 | Завести VK-сообщество | P2 | idea |
| 31 | Настроить email-рассылку (Unisender / SendPulse) — механика дайджеста | P2 | idea |
| 32 | MCP-сервер для аналитики (GSC + Метрика) — чтобы данные были доступны прямо в сессии | P2 | idea |

---

## 🧪 Развитие контента — новые форматы

| # | Задача | Приоритет | Статус |
|---|---|---|---|
| 33 | Освежить кластеры новыми форматами. **Запланировано:** обсуждение 2026-05-19 свело 25 идей в шорт-лист из 5 форматов (F2 сценарные гайды, F13 журнал изменений, F16 квизы, F22 Lottie-анимация) + **F26 интерактивные блоки в статьях как сквозной слой** (вдохновение: notdotteam.github.io/trust, molyanov.ru/blog/markov-chains). **Итерация 2 (2026-05-19, вечер):** F26-7 (симулятор ТС ПИоТ) поднят из Спринта C в Спринт A как **флагман** — статья, где интерактив и есть объяснение, не «статья + калькулятор». Roadmap: A 3 нед / B 3 нед / C 2 нед. Сценарий флагмана — `src/content/wiki/flagship-ts-piot-simulator.md`. | P1 | planned |
| 65 | **Спринт A контент-форматов** (#33). **Пересмотрено 2026-05-19:** добавлен флагман — F26-7 пошаговый симулятор `/kak-rabotaet-ts-piot/` (как пример Markov), сдвинут из Спринта C. Скоуп: (1) **флагман** — лонгрид со сценарием 5–7 шагов «как живёт чек в ТС ПИоТ»; (2) `/zakon-2026/` трекер изменений + фильтр-таймлайн; (3) вторичные калькуляторы — НДС/УСН в `nalogi-msb-2026`, штраф в `shtraf-za-markirovku`. 3 недели (было 2). Блокер: сценарий флагмана в `src/content/wiki/flagship-ts-piot-simulator.md` — до кода. Инфра: `src/components/interactive/`, `src/utils/track.ts`, цели в Метрике (по шагам флагмана + по калькуляторам). | P1 | planned |
| 66 | **Спринт B контент-форматов** (#33): 2 сценарных гайда (F2: «Открываю кофейню за 30 дней», «Перехожу с ИП на ООО за 60 дней») с чек-бокс-симулятором и ROI-калькулятором + квиз F16 «Готов ли бизнес к ТС ПИоТ» с маршрутизацией ответов. 3 недели. Блокер: расчётные формулы ROI ИП vs ООО — нужна проверка юристом. | P1 | planned |
| 67 | **Спринт C контент-форматов** (#33). **Пересмотрено 2026-05-19:** F26-7 (симулятор ТС ПИоТ) переехал в Спринт A как флагман. Остаётся: F22 Lottie-анимация как тизер на pillar-странице, ведущий на флагман + F26-8 drag-and-drop «цепочка ЭДО при маркировке». 2 недели (было 3). Блокер: решение по Lottie — если флагман Спринта A зашёл, Lottie можно отложить. | P1 | planned |

### Задача 33 — направления для проработки

Контент-план сейчас построен на классических информационных статьях: «что такое», «как подключить», «штрафы за». Это даёт трафик, но легко копируется и не отстраивает портал. Нужно подумать, какие форматы добавить — в идеале такие, которые одновременно дают SEO-вес, удерживают на странице и закрывают конкретный сценарий пользователя.

**Решено 2026-05-19:** см. `docs/content-formats-roadmap.md`. Краткая сводка:

- **Выбрано для реализации:** F2 (сценарные гайды), F13 (журнал изменений), F16 (квизы), F22 (Lottie-анимация), **F26 (интерактивные блоки в статьях — сквозной слой над остальными)**.
- **Не выбрано пока:** F1 (инструменты), F3 (фильтры), F4 (кейсы), F5 (explainer), F6 (Q&A), F11 (глоссарий), F12 (обзоры провайдеров), F15 (ROI), F18 (mind maps), F25 (PDF). Часть переплетается с выбранным (F1 и F15 реализуются через F26 внутри статей; F4 откладывается — возможно вернёмся после Спринта A).
- **Не делаем:** F8 шаблоны, F9 видео, F10 партнёрство с ведомствами, F19 чат с экспертом, F20 подкаст, F21 Excel, F23 слайдер, F24 email-курс — низкий ROI / высокая сложность на старте.

**Изначальный список 10 направлений** (для истории дискуссии):

1. **Интерактивные инструменты под кластер** — по образцу уже существующих `/kalkulyator-shtrafov/` и `/kalendar-markirovki/`. Кандидаты:
   - Подбор кассы по сценарию бизнеса (кластер ККТ): ниша → оборот → маркировка → 2–3 модели.
   - Калькулятор УСН/НДС с 2026 (выбор ставки 5/7/20%).
   - Симулятор перехода ИП → ООО с расчётом налоговой разницы.
   - Чек-лист готовности к ТС ПИоТ с проверкой по шагам.
   - Подбор провайдера ТС ПИоТ / ОФД по параметрам.

2. **Сценарные гайды** вместо «что такое X» — путь под конкретную ситуацию: «Открываю кофейню — что мне нужно за 30 дней», «Перехожу с ЕНВД на УСН в 2026», «Получил требование ФНС — пошаговый ответ». Линейный таймлайн с чек-боксами.

3. **Сравнительные таблицы с фильтрами** — React-компонент (как `BlogFilter.tsx`): сравнение моделей касс, ФН, ОФД, банков для ИП по параметрам с реальной фильтрацией.

4. **Разборы реальных кейсов** — анонимизированные истории МСБ с цифрами: штрафы, споры с ФНС, переход на маркировку. Source: судебная практика sudact.ru, разъяснения, кейсы из Telegram-каналов. Формат — «ситуация → ошибка → последствия → как было правильно».

5. **Визуальные explainer-материалы** — схемы дата-флоу (как чек идёт от кассы → ОФД → ФНС → ТС ПИоТ → ЧЗ), таймлайны нормативных изменений, инфографика по штрафам. Картинки от OpenRouter/FLUX уже есть в инфраструктуре.

6. **Q&A-блоки под AI Overviews** — структурированные FAQ-секции с короткими ответами (40–60 слов) под цитирование в ChatGPT/Perplexity/Google AI. Уже частично умеет `/blog geo`.

7. **Обновляемые «живые» страницы** — одна страница, которая постоянно актуализируется: «Календарь маркировки 2026» (уже есть), «Текущие лимиты УСН», «Актуальные штрафы за маркировку». Дата `updatedDate` + явная плашка «обновлено DD.MM».

8. **Шаблоны документов** — заявления, уведомления в ФНС, чек-листы для скачивания. Лид-магниты с осторожностью: только если есть правовая проверка.

9. **Видео/аудио** — короткие YouTube-разборы по 3–5 мин на статью, подкаст-эпизоды. `/blog audio` через Gemini TTS уже подключён, но требует Google API key.

10. **Партнёрские разборы с ФНС/ЧЗ** — официальные комментарии ведомств под публикацию. Долгий, но сильно поднимает E-E-A-T.

**+ 15 идей расширенного списка (2026-05-19):** F11 глоссарий-карточки, F12 длинные обзоры провайдеров, F13 журнал изменений, F14 ежемесячный дайджест, F15 ROI-калькуляторы, F16 тесты, F17 база FAQ-блоков, F18 mind maps, F19 Telegram-чат с экспертом, F20 подкаст-сериал, F21 Excel-шаблоны, F22 Lottie-анимация, F23 слайдер до/после, F24 email-курс, F25 PDF-гайды лидмагниты, **F26 интерактивные блоки в статьях**.

**Открытые вопросы перед Спринтом A** (см. `docs/content-formats-roadmap.md` для полного списка):
- Кластер для пилота — ts-piot или markirovka?
- Категория `/instrumenty/` отдельно или интерактивы внутри статей?
- Дизайн интерактивов — наши --pink/--lime/--sand или внешний дизайнер?
- Аналитика интерактивов через цели в Метрике вручную?
- F22 Lottie — готовая или с нуля?

---

## ✅ Выполнено

| # | Задача | Дата |
|---|---|---|
| — | Base path для GitHub Pages (все ссылки через `u()`) | 2026-05-05 |
| — | Подключён `@astrojs/sitemap` | 2026-05-05 |
| — | Исправлена схема `heroImage`: `image()` → `z.string()` | 2026-05-05 |
| — | ToV-аудит и зачистка слов-паразитов по всем статьям | 2026-05-05 |
| — | Аудит документации: 25 расхождений устранено | 2026-05-05 |
| — | 4 процесса контент-операций: `/plan-content`, `/create-article`, `/release-article`, `/maintain-content` | 2026-05-05 |
| — | Определены CSS-переменные `--gray-dark` и `--gray-light` | 2026-05-05 |
| 1 | Подключён домен etiketka-media.ru к GitHub Pages (`public/CNAME`, `SITE_URL`, `astro.config.mjs:site`) | 2026-05-17 |
| 2 | Сайт верифицирован в Google Search Console | 2026-05-17 |
| 3 | Сайт верифицирован в Яндекс.Вебмастере | 2026-05-17 |
| 4 | Установлен счётчик Яндекс.Метрики (id 109130279, webvisor + clickmap) в `BaseHead.astro` | 2026-05-17 |
| 5 | Добавлены `yandex-verification` и `google-site-verification` в `BaseHead.astro` | 2026-05-17 |
| 12 | Двойной sitemap устранён — остался только кастомный `src/pages/sitemap.xml.ts`, зависимость `@astrojs/sitemap` удалена из `package.json` | 2026-05-17 |
| 13 | `<link rel="sitemap" href={u('/sitemap.xml')} />` добавлен в `BaseHead.astro` | 2026-05-17 |
| 20 | Опубликованы 4 статьи: `usn-nds-rasschet`, `shtraf-za-markirovku`, `ts-piot-evotor-atol-shtrih`, `strahovye-vznosy-msp-2026` (`draft: false`) | 2026-05-17 |
| 21 | Hero-изображение для `usn-nds-rasschet` сгенерировано; все 83 статьи имеют `heroImage` | 2026-05-17 |
| 25 | Code-аудит мобильной вёрстки + фиксы: `.prose table` оборачивается в overflow-x на ≤720px, `main` уменьшил padding до `1.5em 1em` на мобиле, `<pre>` уменьшил padding. Визуальная проверка в браузере — отдельной задачей 25b | 2026-05-17 |
| 26 | 404-страница: добавлена строка поиска (GET-форма → `/search/?q=…`) и блок «Популярные материалы» (`POPULAR_POSTS` в `consts.ts` — курируемый список из 4 ключевых статей) | 2026-05-17 |
| 27 | Прогресс-бар чтения (`#rp-bar` в `BlogPost.astro`, фиксированный sticky-бар + кнопка «Наверх») — уже было в коде, помечено как done | 2026-05-17 |
| 34 | `.claude/commands/cluster-gaps.md` синхронизирован с реальным контент-планом — таблица из 18 кластеров (ключи: `ts-piot`, `markirovka`, `zakonodatelstvo`, `ofd-fn`, `merkuriy`, `kkt`, `markirovka-2026`, `egais`, `buhgalteriya`, `otchetnost-edo`, `uslugi`, `horeca`, `start`, `marketplace`, `kadry`, `banki`, `nichi`, `nalogi`). `find-topics.md` обновил argument-hint и поисковые шаблоны под каждый блок кластеров | 2026-05-17 |
| 35 | В `cluster-gaps.md` расписаны эталонные группы запросов для всех новых кластеров 4–18 (ОФД/ФН, Меркурий, ККТ-модели, маркировка-2026 по датам, ЕГАИС-общепит, бухгалтерия, ЭДО/КЭДО/МЧД, услуги, HoReCa, старт бизнеса, маркетплейсы, кадры, банки, нишевые, налоги) | 2026-05-17 |
| 36 | `rss-sources.md`: добавлены 4 новые папки — Маркетплейсы (WB Seller, Ozon Seller, Я.Маркет, E-Pepper, Retailer), HoReCa (Foodbay, Restoclub, HoReCa Magazine, Frontoffice), Кадры/СФР (СФР, Минтруд, hh.ru, Право.ру, Кадровое дело, Гарант — трудовое), Банки/115-ФЗ (ЦБ РФ, Росфинмониторинг, Банки.ру, Bankir.ru). Расширил Яндекс.Новости-мета-агрегатор: WB-налоги, Ozon-селлер, самозанятые, СФР ЕФС-1, 115-ФЗ, лицензия на алкоголь | 2026-05-17 |
